import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  main: {flexDirection: 'row', alignItems: 'center'},
  starView: {marginRight: 2},
  rating: {marginLeft: 5},
});

export default styles;
